# Copy this file to .env and fill in your values (never commit the real .env).
# Get a free Gemini API key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Vision-capable Gemini model (gemini-2.5-flash is verified working / free tier).
MODEL=gemini-2.5-flash

# Optional fallback: used ONLY when Gemini is rate-limited (429).
# GitHub Models token for gpt-4o-mini. Leave blank to disable the fallback.
GITHUB_TOKEN=your_github_models_token_here
FALLBACK_MODEL=openai/gpt-4o-mini

# Local dev server port
PORT=3001
